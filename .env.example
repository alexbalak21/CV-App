FLASK_APP=wsgi.py
FLASK_CONFIG=dev
SECRET_KEY=change-me-to-a-random-value

# MySQL (matches `flask db upgrade` tested setup: db=cv_app, user=root, empty password)
DATABASE_URL=mysql+pymysql://root:@localhost/cv_app?charset=utf8mb4

# SQLite (default if DATABASE_URL is unset/commented out)
# DATABASE_URL=sqlite:///instance/app.db

# PostgreSQL (future option, see plan.md)
# DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/cvapp
